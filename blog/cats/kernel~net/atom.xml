<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: kernel~net | kk Blog —— 通用基础]]></title>
  <link href="http://abcdxyzk.github.io/blog/cats/kernel~net/atom.xml" rel="self"/>
  <link href="http://abcdxyzk.github.io/"/>
  <updated>2020-10-18T21:52:02+08:00</updated>
  <id>http://abcdxyzk.github.io/</id>
  <author>
    <name><![CDATA[kk]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[TCP_NEW_SYN_RECV]]></title>
    <link href="http://abcdxyzk.github.io/blog/2020/09/10/kernel-tcp-new-syn-recv/"/>
    <updated>2020-09-10T04:40:00+08:00</updated>
    <id>http://abcdxyzk.github.io/blog/2020/09/10/kernel-tcp-new-syn-recv</id>
    <content type="html"><![CDATA[<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=10feb428a5045d5eb18a5d755fbb8f0cc9645626">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=10feb428a5045d5eb18a5d755fbb8f0cc9645626</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=d34ac51b76e8c7de6094cfb11780ef9c2b93469f">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=d34ac51b76e8c7de6094cfb11780ef9c2b93469f</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=4e9a578e5b6bdfa8b7fed7a41f28a86a7cffc85f">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=4e9a578e5b6bdfa8b7fed7a41f28a86a7cffc85f</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=079096f103faca2dd87342cca6f23d4b34da8871">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=079096f103faca2dd87342cca6f23d4b34da8871</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=2215089b224412bfb28c5ae823b2a5d4e28a49d7">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=2215089b224412bfb28c5ae823b2a5d4e28a49d7</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=26e3736090e1037ac929787df21c05497479b77f">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=26e3736090e1037ac929787df21c05497479b77f</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=85645bab57bfc6b0b43bb96a301c4ef83925c07d">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=85645bab57bfc6b0b43bb96a301c4ef83925c07d</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=a9407000038805e5215a49c0a50c9e2b2ff38220">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=a9407000038805e5215a49c0a50c9e2b2ff38220</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=8b5801477926a2b018afc84a53c0b8818843fe73">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=8b5801477926a2b018afc84a53c0b8818843fe73</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=a8399231f0b6e72bc140bcc4fecb0c622298a6bd">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=a8399231f0b6e72bc140bcc4fecb0c622298a6bd</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=caf3f2676aaad395903d24a54e22f8ac4bc4823d">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=caf3f2676aaad395903d24a54e22f8ac4bc4823d</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=4bdc3d66147b3a623b32216a45431d0cff005f50">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=4bdc3d66147b3a623b32216a45431d0cff005f50</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=c2f34a65a61cd1ace3b53c93e8b38d2f79f4ff0d">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=c2f34a65a61cd1ace3b53c93e8b38d2f79f4ff0d</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=f03f2e154f52fdaa982de7e2c386737679963dc9">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=f03f2e154f52fdaa982de7e2c386737679963dc9</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=fff1f3001cc58b5064a0f1154a7ac09b76f29c44">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=fff1f3001cc58b5064a0f1154a7ac09b76f29c44</a></p>

<p><a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=aac065c50aba0c534a929aeb687eb68c58e523b8">https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/commit/?id=aac065c50aba0c534a929aeb687eb68c58e523b8</a></p>

<p>结合以上patch，在 <a href="http://buildlogs.cdn.centos.org/c7.1708.u/kernel/20171204203818/3.10.0-693.11.1.el7.x86_64/kernel-3.10.0-693.11.1.el7.x86_64.rpm">kernel-3.10.0-693.11.1.el7.src.rpm</a> 内核上引入 TCP_NEW_SYN_RECV patch</p>

<p>好处：mptcp和4.15.0的基本一样。</p>

<p>不再需要spin_lock(listen_sk)，最大的互斥变成atomic。（去除atomic看 <a href="/blog/2018/09/28/kernel-sk_lookup/">tcp连接查找</a> ）</p>

<h4>ipv6_addr_v4mapped</h4>

<p>sk_ehashfn 被 ipv4, ipv6 共用，req hash 的时候可用的变量不多，用的是 ipv6_addr_v4mapped(&amp;sk->sk_v6_daddr) 判断是否mapped，所以原先的sk->sk_daddr = addr; sk->sk_rcv_saddr = addr; 换成下面两个函数。
```
    static inline void sk_daddr_set(struct sock <em>sk, __be32 addr)
    {
        sk->sk_daddr = addr; /</em> alias of inet_daddr */
    #if IS_ENABLED(CONFIG_IPV6)
        ipv6_addr_set_v4mapped(addr, &amp;sk->sk_v6_daddr);
    #endif
    }</p>

<pre><code>static inline void sk_rcv_saddr_set(struct sock *sk, __be32 addr)
{
    sk-&gt;sk_rcv_saddr = addr; /* alias of inet_rcv_saddr */
#if IS_ENABLED(CONFIG_IPV6)
    ipv6_addr_set_v4mapped(addr, &amp;sk-&gt;sk_v6_rcv_saddr);
#endif
}
</code></pre>

<pre><code>
#### ir_iif, ireq_net, ireq_state

ir_iif, ireq_net, ireq_state 需要在 req 创建时赋值，因为插入ehash表后的查找需要用到这些变量。

#### reqsk_put

原先部分reqsk_free需要换成reqsk_put，因为req已经和sk一样，靠自己的refcnt维护

#### backlog
</code></pre>

<pre><code>if (sk-&gt;sk_state == TCP_LISTEN) {
    ret = tcp_v4_do_rcv(sk, skb);
    goto put_and_return;
}
</code></pre>

<pre><code>
listen_sk 的包要在tcp_v4_rcv里处理完，不能再加入listen_sk的backlog处理，因为req已经不在listen_sk-&gt;icsk_accept_queue.listen_opt.syn_table里，而backlog(=tcp_v4_do_rcv)又不会再lookup_sk，导致无法找到req。


原来的处理是：按listen_sk的收到包的顺序处理，并且需要spin_lock。按照下面的顺序，即使syn、ack、GET包都在backlog里也能处理(GET包查不到req，能查到establish)。TCP_NEW_SYN_RECV 主要是优化调spin_lock(listen_sk)
</code></pre>

<pre><code>tcp_v4_hnd_req() {
    req = inet_csk_search_req()

    if (req)
        return tcp_check_req()

    nsk = inet_lookup_established()
    if (nsk &amp;&amp; nsk-&gt;sk_state != TCP_TIME_WAIT)
        return nsk;
}
</code></pre>

<p>```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[vpn]]></title>
    <link href="http://abcdxyzk.github.io/blog/2020/09/10/kernel-vpn/"/>
    <updated>2020-09-10T04:39:00+08:00</updated>
    <id>http://abcdxyzk.github.io/blog/2020/09/10/kernel-vpn</id>
    <content type="html"><![CDATA[<h3>方案一</h3>

<p>client &mdash;&ndash; xor;drop &mdash;-> hongkong(squid) &mdash;&mdash;&mdash;&ndash;> server</p>

<h3>方案二</h3>

<p>client &mdash;&ndash; xor;drop &mdash;-> guangzhou(squid) &mdash;&ndash; xor;drop;icmp+mss &mdash;&ndash;> hongkong(squid) &mdash;&mdash;&mdash;> server</p>

<p><a href="/blog/cats/tools~squid/">squid</a></p>

<p><a href="/blog/2020/07/09/kernel-tcp-icmp/">icmp</a></p>

<p>有些时候client和hongkong之间能够ping通，但是tcp就是不通，全被丢弃了，这时需要方案二。</p>

<p>client 和 squid 之间不好使用icmp，因为client大都是在NAT后面，主动发起的icmp能通，但被动的请求进不来。这时需要加上guangzhou中间节点</p>

<p><a href="/blog/2020/07/09/kernel-adjust-mss/">mss</a></p>

<p>为了加icmp头，需要减少TCP_MSS。</p>

<p><a href="/blog/2020/09/10/kernel-digest-xor/">xor</a></p>

<p>drop: 丢弃异常reset、fin等包。</p>

<h3>通过web，实现跨平台随时随地购买使用</h3>

<h4>购买CVM</h4>

<p><a href="/blog/2019/12/12/buy-cvm/">脚本购买CVM</a></p>

<p>web购买CVM:</p>

<pre><code>    &lt;meta http-equiv="Content-Type" content="text/html; charset=utf-8" /&gt;
    &lt;meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no"/&gt;
    &lt;meta name="MobileOptimized" content="320"&gt;
    &lt;meta name="format-detection" content="telephone=no"&gt;
    &lt;meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"&gt;

    &lt;!DOCTYPE html&gt;
    &lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;proxy&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;

    &lt;?php
        $region = "hk";
        if (isset($_GET['region']))
            $region = $_GET['region'];
        $count = "0";
        if (isset($_GET['count']))
            $count = $_GET['count'];
    ?&gt;
    &lt;form action="index.php" method="get"&gt;
    &lt;input name="region" value="&lt;?php echo $region; ?&gt;" style="width:100px"&gt;
    &lt;input name="count" value="0" style="width:100px"&gt;
    &lt;input class="submit" type="submit" value="创建"&gt;
    &lt;/form&gt;
    &lt;?php
        $cmd = "python CVM.py $region $count";

        echo date("Y-m-d H:i:s") . " " . $cmd;
        echo "&lt;/br&gt;&lt;br&gt;";

        exec($cmd);

        if (filesize("show.log") &gt; 0) {
            $myfile = fopen("show.log", "r");
            echo fread($myfile, filesize("show.log"));
            fclose($myfile);
        }
    ?&gt;
    &lt;/body&gt;
    &lt;/html&gt;
</code></pre>

<h4>web配置cache_peer, 方案二中的guangzhou才需要</h4>

<p>cachemgr.cgi 没找到接口，可惜</p>

<h5>填写 IP</h5>

<pre><code>    &lt;meta http-equiv="Content-Type" content="text/html; charset=utf-8" /&gt;
    &lt;meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no"/&gt;
    &lt;meta name="MobileOptimized" content="320"&gt;
    &lt;meta name="format-detection" content="telephone=no"&gt;
    &lt;meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"&gt;

    &lt;!DOCTYPE html&gt;
    &lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;squid&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;

    &lt;?php
        $up = "";
        if (isset($_GET['up']))
            $up = $_GET['up'];
    ?&gt;
    &lt;form action="squid.php" method="get"&gt;
    &lt;input name="up" value="&lt;?php echo $up; ?&gt;" style="width:200px"&gt;
    &lt;input class="submit" type="submit" value="更新"&gt;
    &lt;/form&gt;
    &lt;?php

        echo date("Y-m-d H:i:s");
        echo "&lt;/br&gt;&lt;br&gt;";

        if (strlen($up) &gt; 3 &amp;&amp; strlen($up) &lt; 128) {
            $myfile = fopen("squid.log", "w");
            fwrite($myfile, $up . " up.com\n");
            fclose($myfile);
        }
        $myfile = fopen("squid.log", "r");
        echo fread($myfile, filesize("squid.log"));
        fclose($myfile);
    ?&gt;
    &lt;/body&gt;
    &lt;/html&gt;
</code></pre>

<h5>刷新squid</h5>

<pre><code>    # */1 * * * * cd /var/www/html/; /var/www/html/squid.sh
    # */1 * * * * sleep 30; cd /var/www/html/; /var/www/html/squid.sh
    # chown -R www-data:www-data ../html/
    cat /etc/hosts | grep up.com &gt; hosts_tmp
    diff hosts_tmp squid.log &gt; /dev/null
    if [ $? -eq 0 ]; then exit; fi

    cat /etc/hosts | grep -v up.com &gt; hosts
    cat squid.log &gt;&gt; hosts 
    cp hosts /etc/

    /usr/sbin/squid -k reconfigure -f /root/squid/squid.conf
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[skb 数据异或]]></title>
    <link href="http://abcdxyzk.github.io/blog/2020/09/10/kernel-digest-xor/"/>
    <updated>2020-09-10T04:21:00+08:00</updated>
    <id>http://abcdxyzk.github.io/blog/2020/09/10/kernel-digest-xor</id>
    <content type="html"><![CDATA[<p>skb 数据异或，可以改进成单独的option</p>

<pre><code>    diff --git a/run.sh b/run.sh
    index 138e664..0eb5286 100755
    --- a/run.sh
    +++ b/run.sh
    @@ -19,3 +19,4 @@ echo 10000 &gt; /proc/sys/net/tcp/digest/digest_err_retry
     echo 0 &gt; /proc/sys/net/ipv4/tcp_timestamps # for mptcp+digest

     echo 0 &gt; /proc/sys/net/tcp/mss_adjust
    +echo 0 &gt; /proc/sys/net/tcp/digest/digest_xor
    diff --git a/src/4.15.18/tcp_ipv4.c b/src/4.15.18/tcp_ipv4.c
    index b1b5c9d..b94ba6e 100644
    --- a/src/4.15.18/tcp_ipv4.c
    +++ b/src/4.15.18/tcp_ipv4.c
    @@ -1848,6 +1848,12 @@ int tcp_v4_rcv(struct sk_buff *skb)
        th = (const struct tcphdr *)skb-&gt;data;
        iph = ip_hdr(skb);

    +   if (digest_xor &amp;&amp; digest_skb_check(NULL, NULL, skb, 0) != 0) {
    +       int idx = 0;
    +       skb-&gt;ip_summed = CHECKSUM_UNNECESSARY;
    +       skb_change_bits(skb, th-&gt;doff * 4, skb-&gt;len - th-&gt;doff * 4, &amp;idx);
    +   }
    +
     lookup:
        sk = __inet_lookup_skb(&amp;tcp_hashinfo, skb, __tcp_hdrlen(th), th-&gt;source,
                       th-&gt;dest, sdif, &amp;refcounted);
    diff --git a/src/4.15.18/tcp_ipv6.c b/src/4.15.18/tcp_ipv6.c
    index 3e27967..e975356 100644
    --- a/src/4.15.18/tcp_ipv6.c
    +++ b/src/4.15.18/tcp_ipv6.c
    @@ -1619,6 +1619,12 @@ int tcp_v6_rcv(struct sk_buff *skb)
        th = (const struct tcphdr *)skb-&gt;data;
        hdr = ipv6_hdr(skb);

    +   if (digest_xor &amp;&amp; digest_skb_check(NULL, NULL, skb, 0) != 0) {
    +       int idx = 0;
    +       skb-&gt;ip_summed = CHECKSUM_UNNECESSARY;
    +       skb_change_bits(skb, th-&gt;doff * 4, skb-&gt;len - th-&gt;doff * 4, &amp;idx);
    +   }
    +
     lookup:
        sk = __inet6_lookup_skb(&amp;tcp_hashinfo, skb, __tcp_hdrlen(th),
                    th-&gt;source, th-&gt;dest, inet6_iif(skb), sdif,
    diff --git a/src/4.15.18/tcp_output.c b/src/4.15.18/tcp_output.c
    index dab8703..f766ff4 100644
    --- a/src/4.15.18/tcp_output.c
    +++ b/src/4.15.18/tcp_output.c
    @@ -1118,6 +1118,9 @@ static int __tcp_transmit_skb(struct sock *sk, struct sk_buff *skb,
            oskb = skb;

            tcp_skb_tsorted_save(oskb) {
    +           if (digest_xor)
    +               skb = skb_copy(oskb, gfp_mask);
    +           else
                if (unlikely(skb_cloned(oskb)))
                    skb = pskb_copy(oskb, gfp_mask);
                else
    @@ -1264,6 +1267,13 @@ static int __tcp_transmit_skb(struct sock *sk, struct sk_buff *skb,
            sk_nocaps_add(sk, NETIF_F_GSO_MASK);

            merge = !(tcb-&gt;tcp_flags &amp; TCPHDR_SYN);
    +
    +       if (digest_xor) {
    +           int idx = 0;
    +           skb_change_bits(skb, tcp_header_size, skb-&gt;len - tcp_header_size, &amp;idx);
    +           skb-&gt;ip_summed = CHECKSUM_NONE;
    +           skb-&gt;csum = skb_checksum(skb, tcp_header_size, skb-&gt;len - tcp_header_size, 0);
    +       }
        }

        tcp_options_write((__be32 *)(th + 1), tp, &amp;opts, skb, merge);
    @@ -3122,6 +3132,9 @@ int __tcp_retransmit_skb(struct sock *sk, struct sk_buff *skb, int segs)
            struct sk_buff *nskb;

            tcp_skb_tsorted_save(skb) {
    +           if (digest_xor)
    +               nskb = skb_copy(skb, GFP_ATOMIC);
    +           else
                nskb = __pskb_copy(skb, MAX_TCP_HEADER, GFP_ATOMIC);
                err = nskb ? tcp_transmit_skb(sk, nskb, 0, GFP_ATOMIC) :
                         -ENOBUFS;
    diff --git a/src/digest_core.c b/src/digest_core.c
    index 54af869..50840b1 100644
    --- a/src/digest_core.c
    +++ b/src/digest_core.c
    @@ -419,9 +419,106 @@ out:
        return 0;
     }

    +
    +int digest_xor = 0;
    +unsigned char MY_XOR[65536];
    +
    +int __skb_change_bits(unsigned char *from, int len, int *idx)
    +{
    +   int i;
    +   for (i = 0; i &lt; len; i ++) {
    +       *(from + i) ^= MY_XOR[*idx];
    +       *idx = *idx + 1;
    +   }
    +   return 0;
    +}
    +
    +int skb_change_bits(struct sk_buff *skb, int offset, int len, int *idx)
    +{
    +   int start = skb_headlen(skb);
    +   struct sk_buff *frag_iter;
    +   int i, copy;
    +
    +   if (offset &gt; (int)skb-&gt;len - len)
    +       goto fault;
    +
    +   /* Copy header. */
    +   if ((copy = start - offset) &gt; 0) {
    +       if (copy &gt; len)
    +           copy = len;
    +       // skb_copy_from_linear_data_offset(skb, offset, to, copy);
    +       __skb_change_bits(skb-&gt;data + offset, copy, idx);
    +       if ((len -= copy) == 0)
    +           return 0;
    +       offset += copy;
    +   }
    +
    +   for (i = 0; i &lt; skb_shinfo(skb)-&gt;nr_frags; i++) {
    +       int end;
    +       skb_frag_t *f = &amp;skb_shinfo(skb)-&gt;frags[i];
    +
    +       WARN_ON(start &gt; offset + len);
    +
    +       end = start + skb_frag_size(f);
    +       if ((copy = end - offset) &gt; 0) {
    +           u32 p_off, p_len, copied;
    +           struct page *p;
    +           u8 *vaddr;
    +
    +           if (copy &gt; len)
    +               copy = len;
    +
    +#if LINUX_VERSION_CODE &gt; KERNEL_VERSION(4, 0, 0)
    +           skb_frag_foreach_page(f,
    +                         f-&gt;page_offset + offset - start,
    +                         copy, p, p_off, p_len, copied) {
    +               vaddr = kmap_atomic(p);
    +               //memcpy(to + copied, vaddr + p_off, p_len);
    +               __skb_change_bits(vaddr + p_off, p_len, idx);
    +               kunmap_atomic(vaddr);
    +           }
    +#else
    +           vaddr = kmap_atomic(skb_frag_page(f));
    +           __skb_change_bits(vaddr + f-&gt;page_offset + offset - start, copy, idx);
    +           kunmap_atomic(vaddr);
    +#endif
    +
    +           if ((len -= copy) == 0)
    +               return 0;
    +           offset += copy;
    +       }
    +       start = end;
    +   }
    +
    +   skb_walk_frags(skb, frag_iter) {
    +       int end;
    +
    +       WARN_ON(start &gt; offset + len);
    +
    +       end = start + frag_iter-&gt;len;
    +       if ((copy = end - offset) &gt; 0) {
    +           if (copy &gt; len)
    +               copy = len;
    +           if (skb_change_bits(frag_iter, offset - start, copy, idx))
    +               goto fault;
    +           if ((len -= copy) == 0)
    +               return 0;
    +           offset += copy;
    +       }
    +       start = end;
    +   }
    +
    +   if (!len)
    +       return 0;
    +
    +fault:
    +   return -EFAULT;
    +}
    +
    +
     int digest_init(void)
     {
        int ret;
    +   int i;
    +   unsigned long int next = 1;
    +   for (i = 0; i &lt; 65536; i ++) {
    +       next = next * 2 + 1; // TODO
    +       MY_XOR[i] = next % 251;
    +   }

        ret = digest_trace_init();
        if (ret) {
    diff --git a/src/digest_core.h b/src/digest_core.h
    index 007628c..3ba8c80 100644
    --- a/src/digest_core.h
    +++ b/src/digest_core.h
    @@ -79,6 +79,8 @@ extern atomic_t digest_trace_head_num;
     extern atomic_t digest_trace_free_num;
     extern int digest_trace_log_total;

    +extern int digest_xor;
    +
     extern atomic64_t digest_current, digest_total1, digest_total2, digest_drop_total;
     extern atomic64_t digest_drop_connect;

    diff --git a/src/digest_sysctl.c b/src/digest_sysctl.c
    index 27adfa3..3bfe0ae 100644
    --- a/src/digest_sysctl.c
    +++ b/src/digest_sysctl.c
    @@ -478,6 +478,13 @@ static struct ctl_table digest_sysctl_table[] = {
            .mode = 0644,
            .proc_handler = digest_trace_show_handler,
        },
    +   {
    +       .procname = "digest_xor",
    +       .data = &amp;digest_xor,
    +       .maxlen = sizeof(int),
    +       .mode = 0644,
    +       .proc_handler = proc_dointvec
    +   },
        {}
     };
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[TCP包增一个ICMP头]]></title>
    <link href="http://abcdxyzk.github.io/blog/2020/07/09/kernel-tcp-icmp/"/>
    <updated>2020-07-09T02:00:00+08:00</updated>
    <id>http://abcdxyzk.github.io/blog/2020/07/09/kernel-tcp-icmp</id>
    <content type="html"><![CDATA[<p>发送加头，接收解头。checksum失效，需要额外处理</p>

<pre><code>    #include &lt;linux/module.h&gt;
    #include &lt;linux/kernel.h&gt;
    #include &lt;linux/ip.h&gt;
    #include &lt;linux/tcp.h&gt;
    #include &lt;linux/sctp.h&gt;
    #include &lt;linux/icmp.h&gt;
    #include &lt;linux/slab.h&gt;

    #include &lt;net/ip.h&gt;
    #include &lt;net/tcp.h&gt;
    #include &lt;net/udp.h&gt;
    #include &lt;net/icmp.h&gt;                   /* for icmp_send */
    #include &lt;net/route.h&gt;
    #include &lt;net/ip6_checksum.h&gt;
    #include &lt;net/netns/generic.h&gt;      /* net_generic() */

    #include &lt;linux/netfilter.h&gt;
    #include &lt;linux/netfilter_ipv4.h&gt;

    #ifdef CONFIG_IP_VS_IPV6
    #include &lt;net/ipv6.h&gt;
    #include &lt;linux/netfilter_ipv6.h&gt;
    #include &lt;net/ip6_route.h&gt;
    #endif

    #include &lt;net/ip_vs.h&gt;
    #include &lt;linux/dns_resolver.h&gt;



    #include &lt;linux/module.h&gt;
    #include &lt;linux/kernel.h&gt;
    #include &lt;linux/version.h&gt;
    #include &lt;linux/ip.h&gt;
    #include &lt;linux/tcp.h&gt;
    #include &lt;linux/icmp.h&gt;
    #include &lt;linux/if_arp.h&gt;
    #include &lt;linux/if_ether.h&gt;
    #include &lt;linux/kallsyms.h&gt;
    #include &lt;linux/inetdevice.h&gt;
    #include &lt;linux/netdevice.h&gt;
    #include &lt;net/ip.h&gt;
    #include &lt;net/tcp.h&gt;
    #include &lt;net/udp.h&gt;
    #include &lt;net/icmp.h&gt;
    #include &lt;net/arp.h&gt;
    #include &lt;net/route.h&gt;
    #include &lt;net/neighbour.h&gt;
    #include &lt;net/netevent.h&gt;
    #if (LINUX_VERSION_CODE &gt;= KERNEL_VERSION(2, 6, 32))
    #include &lt;net/net_namespace.h&gt;
    #endif
    #include &lt;linux/netfilter.h&gt;
    #include &lt;linux/netfilter_ipv4.h&gt;

    #include &lt;linux/inet.h&gt;
    #include &lt;linux/skbuff.h&gt;
    #include &lt;linux/kthread.h&gt;

    int icmp_port = 80;
    module_param(icmp_port, int, 0644);

    struct addhdr {
        u32 saddr, daddr;
        u16 sport, dport;
        u16 len;
        u16 magic;
    };

    static unsigned int local_out(void *priv, struct sk_buff *skb, const struct nf_hook_state *state)
    {
        struct iphdr *iph = ip_hdr(skb);
        struct tcphdr *th;
        struct icmphdr *icmp;
        struct addhdr *add;
        int delta;

        iph = ip_hdr(skb);
        th = tcp_hdr(skb);
        if (iph-&gt;protocol != IPPROTO_TCP)
            return NF_ACCEPT;

        if (ntohs(th-&gt;source) != icmp_port &amp;&amp; ntohs(th-&gt;dest) != icmp_port)
            return NF_ACCEPT;

        if (skb-&gt;len + sizeof(struct icmphdr) + sizeof(struct addhdr) &gt; 1500)
            return NF_ACCEPT;

        delta = sizeof(struct icmphdr) + sizeof(struct addhdr) + sizeof(struct ethhdr) - skb_headroom(skb);
        if (delta &gt; 0 &amp;&amp; pskb_expand_head(skb, SKB_DATA_ALIGN(delta), 0, GFP_ATOMIC))
            return NF_ACCEPT;

        iph = ip_hdr(skb);
        th = tcp_hdr(skb);

        if (skb-&gt;ip_summed != CHECKSUM_COMPLETE) {
            th-&gt;check = 0;
            skb-&gt;csum = 0;
            th-&gt;check = tcp_v4_check(skb-&gt;len - ip_hdrlen(skb), iph-&gt;saddr, iph-&gt;daddr, skb_checksum(skb, ip_hdrlen(skb), skb-&gt;len - ip_hdrlen(skb), 0));
            skb-&gt;ip_summed = CHECKSUM_COMPLETE;
        }

        skb_push(skb, sizeof(struct icmphdr) + sizeof(struct addhdr));
        memcpy(skb-&gt;data, skb-&gt;data + sizeof(struct icmphdr) + sizeof(struct addhdr), ip_hdrlen(skb));
        skb_reset_network_header(skb);
        iph = ip_hdr(skb);
        iph-&gt;protocol = IPPROTO_ICMP;
        iph-&gt;tot_len = htons(skb-&gt;len);

        icmp = (struct icmphdr *)(skb-&gt;data + ip_hdrlen(skb));
        icmp-&gt;type = ICMP_ECHO;
        icmp-&gt;code = 0;
        icmp-&gt;un.echo.id = 1;
        icmp-&gt;un.echo.sequence = 1;

        add = (struct addhdr *)(skb-&gt;data + ip_hdrlen(skb) + sizeof(struct icmphdr));
        add-&gt;saddr = iph-&gt;saddr;
        add-&gt;daddr = iph-&gt;daddr;
        add-&gt;sport = th-&gt;source;
        add-&gt;dport = th-&gt;dest;
        add-&gt;len = skb-&gt;len;
        add-&gt;magic = skb-&gt;len;

        skb_set_transport_header(skb, ip_hdrlen(skb));

        icmp-&gt;checksum = 0;
        icmp-&gt;checksum = csum_fold(csum_partial(skb-&gt;data + ip_hdrlen(skb), skb-&gt;len - ip_hdrlen(skb), 0));

        ip_send_check(iph);
        skb-&gt;ip_summed = CHECKSUM_NONE;

        return NF_ACCEPT;
    }

    static unsigned int pre_route(void *priv, struct sk_buff *skb, const struct nf_hook_state *state)
    {
        struct iphdr *iph;
        struct tcphdr *th;
        struct icmphdr *icmp;
        struct addhdr *add;

        if (!pskb_may_pull(skb, sizeof(struct iphdr) + sizeof(struct tcphdr) + sizeof(struct icmphdr) + sizeof(struct addhdr)))
            return NF_ACCEPT;

        iph = ip_hdr(skb);
        if (iph-&gt;protocol != IPPROTO_ICMP)
            return NF_ACCEPT;

        icmp = (struct icmphdr*)(skb-&gt;data + ip_hdrlen(skb));
        if (icmp-&gt;type != ICMP_ECHO || icmp-&gt;code != 0)
            return NF_ACCEPT;

        add = (struct addhdr*)(skb-&gt;data + ip_hdrlen(skb) + sizeof(struct icmphdr));
        th = (struct tcphdr*)(skb-&gt;data + ip_hdrlen(skb) + sizeof(struct icmphdr) + sizeof(struct addhdr));
        if (ntohs(th-&gt;source) != icmp_port &amp;&amp; ntohs(th-&gt;dest) != icmp_port)
            return NF_ACCEPT;

        //if (add-&gt;saddr != iph-&gt;saddr || add-&gt;daddr != iph-&gt;daddr || 
        if (add-&gt;sport != th-&gt;source || add-&gt;dport != th-&gt;dest || add-&gt;len != skb-&gt;len || add-&gt;magic != skb-&gt;len)
            return NF_ACCEPT;

        skb_pull(skb, sizeof(struct icmphdr) + sizeof(struct addhdr));
        // sizeof(struct icmphdr) + sizeof(struct addhdr) &gt; ip_hdrlen(skb)
        memcpy(skb-&gt;data, skb-&gt;data - sizeof(struct icmphdr) - sizeof(struct addhdr), ip_hdrlen(skb));
        memcpy(skb-&gt;data - sizeof(struct ethhdr), skb-&gt;data - sizeof(struct icmphdr) - sizeof(struct addhdr) - sizeof(struct ethhdr), sizeof(struct ethhdr));
        skb_reset_network_header(skb);
        iph = ip_hdr(skb);
        iph-&gt;protocol = IPPROTO_TCP;
        iph-&gt;tot_len = htons(skb-&gt;len);

        ip_send_check(iph);
        skb-&gt;ip_summed = CHECKSUM_UNNECESSARY;
        skb_set_transport_header(skb, ip_hdrlen(skb));
        th = tcp_hdr(skb);
        skb_set_mac_header(skb, -(int)sizeof(struct ethhdr));

        return NF_ACCEPT;
    }

    static const struct nf_hook_ops ip_vs_ops[] = {
        {
            .hook       = local_out,
            .pf     = NFPROTO_IPV4,
            .hooknum    = NF_INET_LOCAL_OUT,
            .priority   = 0,
        },
        {
            .hook       = pre_route,
            .pf     = NFPROTO_IPV4,
            .hooknum    = NF_INET_PRE_ROUTING,
            .priority   = 0,
        },
    };

    static int net_init(void)
    {
        if (nf_register_net_hooks(&amp;init_net, ip_vs_ops, ARRAY_SIZE(ip_vs_ops)))
            return -1;

        return 0;
    }

    static void net_cleanup(void)
    {
        nf_unregister_net_hooks(&amp;init_net, ip_vs_ops, ARRAY_SIZE(ip_vs_ops));
    }

    module_init(net_init);
    module_exit(net_cleanup);
    MODULE_LICENSE("GPL");
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[将包减小到mss以下]]></title>
    <link href="http://abcdxyzk.github.io/blog/2020/07/09/kernel-adjust-mss/"/>
    <updated>2020-07-09T01:57:00+08:00</updated>
    <id>http://abcdxyzk.github.io/blog/2020/07/09/kernel-adjust-mss</id>
    <content type="html"><![CDATA[<pre><code>    diff --git a/src/4.15.18/tcp_output.c b/src/4.15.18/tcp_output.c
    index 82613f5..270545e 100644
    --- a/src/4.15.18/tcp_output.c
    +++ b/src/4.15.18/tcp_output.c
    @@ -52,6 +52,7 @@
     #include "fec_core.h"

     u32 sysctl_post_local = 0xffffff00;
    +int sysctl_mss_adjust = 0;

     static bool tcp_write_xmit(struct sock *sk, unsigned int mss_now, int nonagle,
                   int push_one, gfp_t gfp);
    @@ -1720,6 +1721,8 @@ unsigned int tcp_current_mss(struct sock *sk)
                mss_now = tcp_sync_mss(sk, mtu);
        }

    +   mss_now -= sysctl_mss_adjust;
    +
        header_len = tcp_established_options(sk, NULL, &amp;opts, &amp;md5, getconninfo(sk)) +
                 sizeof(struct tcphdr);
        /* The mss_cache is sized based on tp-&gt;tcp_header_len, which assumes
    @@ -3401,6 +3404,7 @@ struct sk_buff *tcp_make_synack(const struct sock *sk, struct dst_entry *dst,
        skb_dst_set(skb, dst);

        mss = tcp_mss_clamp(tp, dst_metric_advmss(dst));
    +   mss -= sysctl_mss_adjust;

        memset(&amp;opts, 0, sizeof(opts));
     #ifdef CONFIG_SYN_COOKIES
    @@ -3561,6 +3565,7 @@ static void tcp_connect_init(struct sock *sk)
        if (!tp-&gt;window_clamp)
            tp-&gt;window_clamp = dst_metric(dst, RTAX_WINDOW);
        tp-&gt;advmss = tcp_mss_clamp(tp, dst_metric_advmss(dst));
    +   tp-&gt;advmss -= sysctl_mss_adjust;

        tcp_initialize_rcv_mss(sk);

    diff --git a/src/io_sysctl.c b/src/io_sysctl.c
    index c3b2ddd..6fdc1df 100644
    --- a/src/io_sysctl.c
    +++ b/src/io_sysctl.c
    @@ -9,6 +9,7 @@ extern int sysctl_detail;
     extern int sysctl_data_ssthresh;

     extern int sysctl_post_local;
    +extern int sysctl_mss_adjust;

     extern unsigned long total_session;
     extern unsigned long current_session;
    @@ -39,6 +40,13 @@ static struct ctl_table tcp_sysctl_table[] = {
            .mode = 0644,
            .proc_handler = proc_dointvec
        },
    +   {
    +       .procname = "mss_adjust",
    +       .data = &amp;sysctl_mss_adjust,
    +       .maxlen = sizeof(int),
    +       .mode = 0644,
    +       .proc_handler = proc_dointvec
    +   },
        {
            .procname = "total_session",
            .data = &amp;total_session,
</code></pre>
]]></content>
  </entry>
  
</feed>
