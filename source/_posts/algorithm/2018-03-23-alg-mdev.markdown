---
layout: post
title: "遍历数组一次求方差"
date: 2018-03-23 00:41:00 +0800
comments: false
categories:
- 2018
- 2018~03
- algorithm
- algorithm~base
tags:
---

http://cq520.iteye.com/blog/2020648


 一个长度为X的数组，里面存有N个数，能否只遍历数组一次就求得数组的方差。

计算方差的第二个公式：DX^2=EX^2-(EX)^2，代码如下：

```
	public class Deviation {
		/**
		 * 传统的利用平均数求方差的方法,需要遍历数组两次
		 * @param a 目标数组
		 * @return 方差
		 */
		private static double ComputeVariance(double a[]){
			double variance=0;//方差
			double average=0;//平均数
			int i,len=a.length;
			double sum=0,sum2=0;
			for(i=0;i<len;i++){
				sum+=a[i];
			}
			average=sum/len;
			for(i=0;i<len;i++){
				sum2+=(a[i]-average)*(a[i]-average);
			}
			variance=sum2/len;
			return variance;
		}

		/**
		 * 只遍历数组一次求方差，利用公式DX^2=EX^2-(EX)^2
		 * @param a
		 * @return
		 */
		private static double ComputeVariance2(double a[]){
			double variance=0;//方差
			double sum=0,sum2=0;
			int i=0,len=a.length;
			for(;i<len;i++){
				sum+=a[i];
				sum2+=a[i]*a[i];
			}
			variance=sum2/len-(sum/len)*(sum/len);
			return variance;
		}

		public static void main(String args[]){
			double a[]={1,2,3,4,5};
			System.out.println(ComputeVariance(a));
			System.out.println(ComputeVariance2(a));

		}
	}
```

